function Navbar() {
  return (
    <nav>
      <div>
        TASTY BURGER
      </div>

      <div>
        <a href="#">HOME</a>
        <a href="#">MENU</a>
        <a href="#">ABOUT</a>
        <a href="#">CONTACT</a>
      </div>
    </nav>
  )
}

export default Navbar